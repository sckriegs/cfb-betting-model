# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Data
data/raw/
data/processed/
data/features/
# Keep models if they are small enough (<100MB), otherwise ignoring them is better 
# but for this app to work you NEED the models committed or built on fly.
# Let's assume we commit the latest models for now.
# data/models/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Streamlit
.streamlit/secrets.toml
